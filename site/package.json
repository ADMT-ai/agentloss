{
  "name": "agentloss-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static discovery + install site for agentloss (agentloss.com). Serves index.html and a fresh copy of llms.txt.",
  "scripts": {
    "build": "bash build.sh",
    "preview": "npm run build && npx --yes serve public"
  },
  "license": "MIT"
}
