# Build output — regenerated by build.sh. Never commit it: it contains a copy of
# llms.txt (edited concurrently in the repo root) that must stay fresh at build time.
public/

# Vercel local link metadata
.vercel/
.vercel

# Co-located llms.txt: only ever staged here transiently (e.g. by a CLI deploy
# that uploads just this folder). The source of truth is the repo-root llms.txt.
llms.txt
